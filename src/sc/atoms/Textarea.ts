import styled from 'styled-components';

const StyleTextArea = styled.textarea`
  display: block;
  width: 100%;
  border: none;
  border-radius: 0;
  appearance: none;
  resize: none;
  outline: none;
  box-sizing: border-box;
`;

export const Styled = {
  StyleTextArea,
};
